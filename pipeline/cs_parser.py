def parse_cs_file(path: str):
    """
    Temporary stub parser.
    In real version, this will parse C# file and return AST.
    """
    print(f"[Parser] Pretending to parse: {path}")
    return [{"class": "SampleClass", "method": "SampleMethod"}]
